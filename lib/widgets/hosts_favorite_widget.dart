import 'package:flutter/cupertino.dart';

class HostsFavoriteWidget extends StatelessWidget {
  const HostsFavoriteWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No Favorite Hosts'),
    );
  }
}
